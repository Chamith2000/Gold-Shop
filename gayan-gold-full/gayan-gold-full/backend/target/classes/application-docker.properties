# # Profile for running against Docker MySQL
# spring.datasource.url=jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:gayan_gold}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
# spring.datasource.username=${DB_USERNAME:gayan}
# spring.datasource.password=${DB_PASSWORD:gayan_gold_secret}

# spring.jpa.hibernate.ddl-auto=validate
# spring.flyway.enabled=true
